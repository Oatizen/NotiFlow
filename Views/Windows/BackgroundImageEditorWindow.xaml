<Window x:Class="NotiFlow.Views.Windows.BackgroundImageEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="背景图排版画板" Height="650" Width="950"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="320" />
        </Grid.ColumnDefinitions>
        
        <!-- 左侧画板区 -->
        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="可视化排版" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            
            <Border Grid.Row="1" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" ClipToBounds="True">
                <Border.Background>
                    <DrawingBrush Viewport="0,0,16,16" ViewportUnits="Absolute" TileMode="Tile">
                        <DrawingBrush.Drawing>
                            <DrawingGroup>
                                <GeometryDrawing Brush="#D3D3D3">
                                    <GeometryDrawing.Geometry>
                                        <RectangleGeometry Rect="0,0,16,16"/>
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                                <GeometryDrawing Brush="#FFFFFF">
                                    <GeometryDrawing.Geometry>
                                        <GeometryGroup>
                                            <RectangleGeometry Rect="0,0,8,8"/>
                                            <RectangleGeometry Rect="8,8,8,8"/>
                                        </GeometryGroup>
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingGroup>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Border.Background>
                <!-- 实际画板，动态居中显示 -->
                <Viewbox Stretch="Uniform" Margin="20">
                    <Grid x:Name="ArtboardContainer">
                        <Canvas x:Name="ArtboardCanvas" Background="Transparent" ClipToBounds="True">
                            <!-- 裁剪后的图片，支持拖拽 -->
                            <Thumb x:Name="ImageThumb" DragDelta="ImageThumb_DragDelta">
                                <Thumb.Template>
                                    <ControlTemplate>
                                        <Image x:Name="BackgroundImageElement" Stretch="Fill" Source="{Binding}" />
                                    </ControlTemplate>
                                </Thumb.Template>
                            </Thumb>
                        </Canvas>
                    </Grid>
                </Viewbox>
            </Border>
            
            <StackPanel Grid.Row="2" Margin="0,20,0,0">
                <TextBlock Text="动态长度模拟器 (滑动测试长短弹幕适配)" Margin="0,0,0,10" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <Slider x:Name="LengthSimulationSlider" Minimum="10" Maximum="100" Value="100" ValueChanged="LengthSimulationSlider_ValueChanged" />
            </StackPanel>
        </Grid>
        
        <!-- 右侧设置区 -->
        <Border Grid.Column="1" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <StackPanel>
                <Button Content="选择本地图片..." HorizontalAlignment="Stretch" Click="SelectImage_Click" Margin="0,0,0,20" />
                
                <TextBlock Text="九宫格对齐点" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <!-- 3x3 Grid for anchors -->
                <Grid Width="150" Height="150" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <ToggleButton x:Name="AnchorTopLeft" Grid.Row="0" Grid.Column="0" Content="↖" Click="Anchor_Click" />
                    <ToggleButton x:Name="AnchorTopCenter" Grid.Row="0" Grid.Column="1" Content="↑" Click="Anchor_Click" />
                    <ToggleButton x:Name="AnchorTopRight" Grid.Row="0" Grid.Column="2" Content="↗" Click="Anchor_Click" />
                    
                    <ToggleButton x:Name="AnchorMiddleLeft" Grid.Row="1" Grid.Column="0" Content="←" Click="Anchor_Click" />
                    <ToggleButton x:Name="AnchorMiddleCenter" Grid.Row="1" Grid.Column="1" Content="·" Click="Anchor_Click" />
                    <ToggleButton x:Name="AnchorMiddleRight" Grid.Row="1" Grid.Column="2" Content="→" Click="Anchor_Click" />
                    
                    <ToggleButton x:Name="AnchorBottomLeft" Grid.Row="2" Grid.Column="0" Content="↙" Click="Anchor_Click" />
                    <ToggleButton x:Name="AnchorBottomCenter" Grid.Row="2" Grid.Column="1" Content="↓" Click="Anchor_Click" />
                    <ToggleButton x:Name="AnchorBottomRight" Grid.Row="2" Grid.Column="2" Content="↘" Click="Anchor_Click" />
                </Grid>
                
                <TextBlock Text="偏移量计算 (自动绑定拖拽)" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="X 偏移:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    <ui:TextBox x:Name="OffsetXBox" Grid.Column="1" Text="0" TextChanged="OffsetBox_TextChanged" />
                </Grid>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Y 偏移:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    <ui:TextBox x:Name="OffsetYBox" Grid.Column="1" Text="0" TextChanged="OffsetBox_TextChanged" />
                </Grid>
                
                <TextBlock Text="图片缩放比例:" Margin="0,0,0,5" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Slider x:Name="ScaleSlider" Minimum="1" Maximum="400" Value="100" ValueChanged="ScaleSlider_ValueChanged" VerticalAlignment="Center" />
                    <TextBlock x:Name="ScaleText" Grid.Column="1" Text="100%" FontWeight="Bold" Margin="10,0,0,0" Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                </Grid>
                
                <CheckBox x:Name="KeepBaseColorCheckBox" Content="图片未覆盖区域使用弹幕底色" Margin="0,0,0,20" Checked="KeepBaseColor_Changed" Unchecked="KeepBaseColor_Changed" />
                
                <ui:Button Content="保存并应用" Appearance="Primary" HorizontalAlignment="Stretch" Margin="0,0,0,0" Click="Save_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
